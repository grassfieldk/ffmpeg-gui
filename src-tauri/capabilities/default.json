{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow core IPC/events for the main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
